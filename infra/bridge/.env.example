# ═══════════════════════════════════════════════════════════════════════════════
# CodeCapsule Bridge API — Environment Variables
#
# Copy to .env and fill in real values before running docker compose up.
# ═══════════════════════════════════════════════════════════════════════════════

# ── HMAC shared secret for verifying Cloudflare Worker → Bridge requests ──
# Must match WORKER_SHARED_SECRET in the Workers environment (wrangler.toml or CF dashboard).
# Generate: openssl rand -hex 32
WORKER_SHARED_SECRET=

# ── Azure OpenAI credentials for the 3-agent generation pipeline ──
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview
